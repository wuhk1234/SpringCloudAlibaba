<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wuhk.mapper.StockSeataMapper">

    <insert id="creat" parameterType="com.wuhk.entity.StockSeata">
       INSERT INTO seata_stock_tbl (
            productId,
            count
        )
        VALUES
            (
                #{productId},
                #{count}
            )
    </insert>

    <update id="updata" parameterType="Integer">
        UPDATE seata_stock_tbl
        SET count = count - 1
        WHERE
            productId = #{productId}
    </update>
</mapper>
